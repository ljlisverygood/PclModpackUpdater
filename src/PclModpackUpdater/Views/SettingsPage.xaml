<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="PclModpackUpdater.Views.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    NavigationCacheMode="Enabled">

    <ScrollViewer>
        <StackPanel MaxWidth="880" Padding="36,12,36,32" Spacing="16">

            <TextBlock Text="设置"
                       Style="{StaticResource TitleTextBlockStyle}" />

            <Border Style="{StaticResource CardStyle}">
                <StackPanel Spacing="12">
                    <TextBlock Text="外观"
                               Style="{StaticResource SubtitleTextBlockStyle}" />
                    <RadioButtons x:Name="ThemeSelector"
                                  Header="应用主题">
                        <RadioButton Content="跟随系统" />
                        <RadioButton Content="浅色" />
                        <RadioButton Content="深色" />
                    </RadioButtons>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel Spacing="12">
                    <TextBlock Text="更新"
                               Style="{StaticResource SubtitleTextBlockStyle}" />
                    <ToggleSwitch x:Name="AutoCheckSwitch"
                                  Header="启动时自动检查更新"
                                  OnContent="开启"
                                  OffContent="关闭" />
                    <ToggleSwitch x:Name="LaunchAfterSwitch"
                                  Header="下载完成后自动启动 PCL"
                                  OnContent="开启"
                                  OffContent="关闭" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardStyle}">
                <StackPanel Spacing="8">
                    <TextBlock Text="关于"
                               Style="{StaticResource SubtitleTextBlockStyle}" />
                    <TextBlock x:Name="AboutText"
                               TextWrapping="Wrap"
                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    <HyperlinkButton x:Name="RepoLink" Content="GitHub 仓库" />
                </StackPanel>
            </Border>
        </StackPanel>
    </ScrollViewer>
</Page>
